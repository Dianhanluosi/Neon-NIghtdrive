using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lightmove : MonoBehaviour
{

    //move directional light x rotation over time 
    //Quaternion newRotation = Quarternion.idendtity;
    //this.transform.rotation = newRotation.EulerAngles(time.Time/1000f,0,0);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
